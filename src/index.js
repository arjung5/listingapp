import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />
  ,document.getElementById('root')
);


//Read Me First:-

// VehicleList where all data is being fetched
// VehicleLists is the render for particular list value
